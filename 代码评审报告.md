# 金融分析报告生成 Agent · 代码评审报告（Day 1/2）

> 评审日期：2026-08-17
> 评审范围：`jiuwenswarm/resources/agent/workspace/skills/finance-report/`（collectors / common / analyzers 及顶层骨架）+ `tests/unit_tests/finance/` 全部 8 个测试文件
> 评审方式：全量实读代码；沙箱绕过问题已在本机通过 `CodeExecutor.execute()` 原路径实测验证
> 问题统计：CRITICAL 1 / HIGH 3 / MEDIUM 3 / LOW 7

---

## 🔴 CRITICAL（必须立即修复）

### C1. AST 白名单沙箱可被完全绕过，LLM 生成代码可执行任意系统命令 / 读取任意文件

**位置**：`analyzers/code_executor.py` L30-36、L83-91（`check_safety`）

**问题**：`check_safety` 只拦截 `ast.Attribute` 节点上的 dunder 属性与 `func` 恰为 `ast.Name` 且命中黑名单的直接调用。以下三类向量全部穿透（**已实测通过 `execute()` 真实执行**）：

| 绕过向量 | 实测结果 |
|---|---|
| `getattr(obj, "__class__")` 字符串式 dunder 访问（dunder 名在字符串常量里，不是 Attribute 节点） | ✅ 通过校验；`getattr(getattr((), "__class__"), "__base__")` 链实测枚举出 1410 个子类（逃逸入口） |
| `getattr(__builtins__, "__import__")("os")` —— 裸名 `__builtins__` 不是 Attribute 节点 | ✅ 实测在沙箱内成功 `import os` 并打印 `os.getcwd()` |
| `get_ipython().system('...')` / `run_line_magic('bash', ...)` —— IPython `!cmd` 的底层 Python API | ✅ **实测执行了系统命令**（`echo SANDBOX_BYPASS_PROOF` 输出成功） |

另有一条信息泄露通道同样放行：`pd.read_csv(".../.env")` —— `open` 被禁但 pandas 内部打开文件，可读取项目根 `.env`（**含 API Key**）并经 stdout 回传给模型；`pd.read_csv("http://...")` 还可发起任意 URL 请求（SSRF/数据外带）。

正面确认：f-string 内嵌 dunder（`f"{x.__class__}"`）与 IPython `%`/`!` 前缀（`ast.parse` 直接 SyntaxError）确实被拦截；comprehension/lambda 内的调用也会被 `ast.walk` 覆盖。问题只出在动态访问与 IPython API。

**修复建议**（组合拳，AST 层至少做 1-3）：

1. 黑名单扩充：拦截动态访问原语与 IPython/dunder 名字：

```python
BLOCKED_BUILTINS |= {"getattr", "setattr", "delattr", "vars", "dir",
                     "type", "super", "object", "help", "exit", "quit"}
BLOCKED_NAMES = {"__builtins__", "__import__", "get_ipython",
                 "__loader__", "__spec__", "breakpoint"}
```

2. `check_safety` 新增两类节点检查：

```python
elif isinstance(node, ast.Name) and node.id in self.BLOCKED_NAMES:
    return False, f"禁止引用名字 '{node.id}'"
elif isinstance(node, ast.Constant) and isinstance(node.value, str) \
        and node.value.startswith("__"):
    return False, f"禁止双下划线字符串常量 '{node.value}'"  # 拦 getattr 字符串式 dunder
```

3. 静态白名单理论上无法穷尽（`"{0.__class__}".format(x)`、`operator.attrgetter` 等仍可构造），生产环境必须叠加**进程级隔离**：在受限子进程/容器中运行 shell（无 `.env`、无敏感环境变量、限制网络与文件系统），AST 校验只作为第一道闸。

---

## 🟠 HIGH（应当修复）

### H1. 三条行情源的成交量单位不一致：主路径为"手"、降级路径为"股"，相差 100 倍

**位置**：`collectors/quote_collector.py` L144-211

**问题**：`_fetch_tencent` 注释明确"量(手)"并做 `v * 100` 转股，新浪日线 volume 为股，即两条降级路径输出**股**；而主路径 akshare `stock_zh_a_hist` 的"成交量"单位是**手**，`float(row["成交量"])` 未换算。哪个源成功决定了 volume 差 100 倍——任何基于成交量的换手率/量能分析在不同运行之间不可比（量纲类 bug 翻版，且被 `source` 字段的"可复现"承诺掩盖）。

**修复**：统一口径为股：`volume=float(row["成交量"]) * 100`；并在 `QuoteRecord`/`QuoteData` 注释中写明单位。

### H2. 同比增速在缺上年同期时"回退上一期"，输出跨季失真的假同比

**位置**：`analyzers/finance_analyzer.py` L118-128（`_yoy_prev`）

**问题**：上年同期缺失（如仅有 `2025-Q1` 起的数据）时回退 `statements[-2]`（上一报告期）。A 股季报为累计口径：最新期 `2026-Q1` 与上一期 `2025-Q4`（年报）对比，营收会算出约 -87% 的"同比下滑"，并以洞察文案输出——数值与定性都错。上市不足两年或 `periods<5` 时必然触发。

**修复**：上年同期缺失时不计算增速（`growth` 保持空并附洞察"历史报告期不足，无法计算同比"）；如确需环比，单列 `qoq_growth` 字段并在洞察文案中明确区分。

### H3. RAG 索引写入非原子、加载无损坏恢复——一次中断即永久崩溃

**位置**：`collectors/rag_retriever.py` L410-423（`_save_index`/`_load_index`）

**问题**：`_save_index` 直接 `open("w")` 覆写 `index.json`，进程被杀/断电时留下截断 JSON；`_load_index` 的 `json.load` 无异常处理，之后每次 `retrieve()`/`add_documents()` 都抛 `JSONDecodeError`，知识库功能永久失效直到人工删文件。对比 `LongTermMemory._load`（hybrid_memory.py）有损坏重建逻辑，同类组件行为不一致。

**修复**：写临时文件后 `os.replace()` 原子替换；加载损坏时记警告并 `build()` 重建。

---

## 🟡 MEDIUM（建议修复）

### M1. 缺失财务数据以 0.0 为哨兵值，污染推导与洞察

**位置**：`collectors/filing_collector.py` L115-135（`value()`）、`analyzers/finance_analyzer.py` L177-181

**问题**：`value()` 对缺失/非数值一律返回 `0.0`。后果：(a) ROE 缺失时 `roe=0` 且 `net_profit>0` → 生成"ROE 0.0%，股东回报偏弱"的错误洞察；(b) `debt_ratio` 缺失为 0 → `total_assets = equity/(1-0) = equity`，资产负债结构推导整体失真。akshare 部分指标缺行是常态路径。

**修复**：`value()` 缺失返回 `None`，`FinancialStatement` 相应字段改 `Optional[float]`；洞察与 `total_assets` 推导处显式跳过 `None`。

### M2. 搜狗反爬退避最坏累计 40 秒纯 sleep，且末次失败后仍空等

**位置**：`collectors/news_collector.py` L268-277

**问题**：`for attempt in range(4)` 中 `time.sleep(4 * (attempt + 1))` 在最后一次尝试失败后仍执行（4+8+12+16=40s）。该退避按"每查询"计：3 个初始查询 × 最多 3 轮深度，搜狗持续反爬时单个关键词 Deep Research 仅空等就可能超过 2 分钟，批量跑 49 家标的不可接受。

**修复**：最后一次失败后不再 sleep；另建议给 `collect()` 增加整体时间预算（如 120s）兜底。

### M3. `WriterAgent` 向无参 `ReportWriter` 传 config，company 路径当前一跑即 TypeError

**位置**：`agents/writer.py` L25-28、`generators/report_writer.py`

**问题**：`ReportWriter(self.config)`，但 `ReportWriter` 类没有定义 `__init__` 不接受参数 → `TypeError`。`orchestrator.generate()` → `writer.write()` 是已接线的真实调用链，`python run_report.py company --target 600519` 现在必然在阶段 3 崩溃。这不是 TODO 占位，是接线错误。

**修复**：给 `ReportWriter` 加 `def __init__(self, config: Optional[dict] = None)`，或调用处改为 `ReportWriter()`。

---

## 🟢 LOW（可择机处理）

| 编号 | 位置 | 问题 | 修复建议 |
|---|---|---|---|
| L1 | `generators/report_writer.py` L17-18 | `from ..analyzers...` 跨包相对导入违反"技能内绝对导入"约定（唯一遗漏处） | 改绝对导入 + ImportError 兜底 |
| L2 | `orchestrator.py` L104-111 | `save_report` 写死子目录"个股投资研报"，行业/宏观报告也走该函数，与提交目录规范语义冲突 | 按 `report_type` 分目录 |
| L3 | `common/hybrid_memory.py` `ingest(kind="conclusion")` | 空壳 `CompanySummary` 整条覆盖同 symbol 记忆，丢失此前 name/sector/key_metrics | 改"存在则仅更新 conclusion 与 updated_at"的合并语义 |
| L4 | `collectors/rag_retriever.py` | (a) 远端路径下每次 `add_documents` 全库重向量（API 成本 O(N²)）；(b) `if not merged` 死代码；(c) `_load_index(create)` 参数从未使用 | 增量只向量化新块（成本优化）；删死代码 |
| L5 | `analyzers/code_executor.py` L105 | `InteractiveShell.instance()` 进程级单例，多实例共享 user_ns 变量互窜；`reset()` 未重导 matplotlib | 并发场景改 `InteractiveShell()` 独立实例 |
| L6 | `agents/investor.py` L111-117 | `_allocate` 缩放后逐项 `round(w, 2)` 可能使权重和 > 1.0（如 0.34×3=1.02），违反提交硬约束 | 最后一项用 `1 - sum(其余)` 收尾 |
| L7 | 测试覆盖缺口 | (a) `llm_client.py` 零测试；(b) 腾讯/新浪降级解析器无测试（H1 正落盲区）；(c) RAG 持久化→新实例加载链路未测（H3 盲区）；(d) code_executor 无动态访问绕过的回归用例（C1 盲区）；(e) news_collector 三个 HTML/JSON 解析器未测 | Day 3 起对每个降级分支补"假响应解析"用例；C1 修复后固化 `getattr/__builtins__/get_ipython` 三类 payload 为用例 |

---

## ✅ 正面确认（重点排查过、无问题的项）

- **PE 量纲**：`market_cap(亿元)×1e8`、`price×total_shares(亿股)×1e8`、净利润统一为元、Q1×4/Q2×2/Q3×4/3 年化——此前"市值亿元 vs 净利润元"bug 已正确修复，且有 25.0/200.0 两个精确断言用例锁住；
- **API Key 安全**：llm_client 与 rag_retriever 的 Key 均只进请求头（`x-api-key` / `Authorization`），不进 URL；日志仅输出模型正文前 200 字符；`raise_for_status` 异常不含 Key。未发现直接泄漏路径（但注意 C1 的 `.env` 读取通道间接构成 Key 泄漏）；
- 降级链、批量单点失败隔离、Deep Research 饱和终止/查询去重的既有测试逻辑与实现吻合；pool_loader 白名单硬校验 + 真实 49 家/6 板块验收测试到位。

---

## 总体评价

- **架构合理性：良好。** 采集层（多源降级+溯源标注）→ 分析层（结构化指标+规则化洞察）→ 编排层（写作-审查反馈环）分层清晰，三板斧设计（混合分块/双路召回/RRF、三层混合记忆、板块即竞对）贴合比赛场景且成本意识好。主要架构性短板是 CodeExecutor 的隔离模型：把"AST 静态白名单"当作沙箱本体，缺少进程级兜底。
- **测试充分性：中上，但有结构性盲区。** Day 1/2 已实现逻辑的测试覆盖面和断言质量都不错（含边界、降级、幂等），但降级路径的真实解析器、RAG 持久化链路、LLM 客户端、以及安全向量恰好都没有测试——本次 3 个 HIGH 中 2 个落在测试盲区，说明测试跟着 happy path 走，建议 Day 3 起对每个降级分支补"假响应解析"用例。
- **安全性：沙箱不合格，其余尚可。** C1 已实测可从 LLM 生成代码执行任意系统命令并读取 `.env`，比赛评审复跑或模型被污染输出时即是现实风险，建议立即修复（黑名单扩充 + 常量/名字检查是十行以内改动，进程隔离随后补）。

## 修复优先级

```
C1（立即：沙箱绕过）→ H1/H2（数据正确性，评审演示会暴露）→ H3/M3（稳定性）→ 其余随 Day 3 迭代处理
```

---

## 🔧 修复状态（2026-08-17）

> 全量回归：**125 passed**（评审时 102，新增 23 个回归用例锁住全部修复项）
> 真实数据冒烟：600519 落盘财报全链路正常（营收同比 +1.3%、净利润同比 -1.95%、4 条洞察，无 0.0 哨兵污染）

| 编号 | 状态 | 修复内容 |
|---|---|---|
| C1 | ✅ 已修复 | `BLOCKED_BUILTINS` 扩入 getattr/setattr/delattr/dir/type/super/object/help/exit/quit；新增 `BLOCKED_NAMES`（`__builtins__`/`get_ipython` 等）与双下划线字符串常量检查、str.format 格式串拦截；评审实测的全部绕过向量已拦截并固化为 9 个回归用例。进程级隔离按建议纳入后续迭代（类注释已说明 AST 仅第一道闸） |
| H1 | ✅ 已修复 | akshare 主路径成交量 ×100 换算为股，与腾讯/新浪降级源统一；QuoteRecord 注明单位；回归用例锁定 25000 手→2500000 股 |
| H2 | ✅ 已修复 | `_yoy_prev` 缺上年同期返回 None，不再回退上一期；growth 为空时输出“历史报告期不足（缺上年同期），无法计算同比增速”洞察 |
| H3 | ✅ 已修复 | `_save_index` 临时文件 + `os.replace` 原子替换；`_load_index` 损坏时警告并从 docs 自动重建；持久化→新实例加载、损坏重建两个用例覆盖 |
| M1 | ✅ 已修复 | `value()` 缺失/非数值/非有限值返回 None；FinancialStatement 全字段 `Optional[float]`；分析端（ROA/现金流比/同比/PE/洞察）显式跳过 None；3 个回归用例（ROE 缺失不误报“回报偏弱”、负债率缺失不推导总资产） |
| M2 | ✅ 已修复 | 搜狗第 4 次尝试失败后不再 sleep（最坏省 16s/查询）；`collect()` 新增 `time_budget` 整体预算（默认 120s，可配置），超时提前终止并写入 `search_trace.budget_exceeded` |
| M3 | ✅ 已修复 | ReportWriter 增加 `__init__(config)`，WriterAgent→ReportWriter 传参链路打通（company 路径端到端用例） |
| L1 | ✅ 已修复 | report_writer.py 相对导入改绝对导入 + sys.path 兑底（技能内导入约定最后一个遗漏处） |
| L2 | ✅ 已修复 | `save_report` 按 report_type 分目录（个股投资研报/行业研报/宏观研报，兑底“其他研报”） |
| L3 | ✅ 已修复 | `ingest(kind="conclusion")` 对已存在 symbol 改合并语义：仅更新 conclusion 与 updated_at，保留 name/sector/key_metrics/insights |
| L4 | ✅ 已修复 | add_documents 增量化：远端路径只向量化新块（消除 O(N²) API 成本）；删除 `if not merged` 死代码与 `_load_index(create)` 死参数 |
| L5 | ✅ 已修复 | `reset()` 重导 numpy/pandas/matplotlib + 字体配置；单例限制已在类/方法注释说明，独立实例/进程隔离随并发改造跟进 |
| L6 | ✅ 已修复 | `_allocate` 缩放后 round 累计误差由末位吸收（`1 - sum(其余)`），权重和恒 ≤ 1.0；用例锁定 100/97/97 场景 |
| L7 | 🟡 部分处理 | 本轮已补：C1 绕过向量 9 例、H1 主路径单位、H3 持久化链路；腾讯/新浪降级解析器、llm_client、news HTML 解析器用例随 Day 3 每个降级分支补齐 |
