# finance-report 项目优化报告

> 输入来源：
> 1. 《TradingAgents 架构分析与对比报告》（工作区根目录，2026-08-20）——开源成熟框架的 11 项可借鉴点
> 2. 得物技术《得物知识问答：复合检索 Agent 的系统设计实践》（微信公众号，2026-08-12）——生产级检索 Agent 的系统设计方法论
>
> 优化对象：`jiuwenswarm/resources/agent/workspace/skills/finance-report/`（CCF BDCI 2026 赛题二参赛技能）
> 时间约束：初赛截止 2026-08-23，优化项按时间窗口分级

---

## 一、现状盘点与瓶颈诊断

当前系统五阶段链路（选股 → 采集 → 分析 → 决策 → 报告）已完整闭环，241+ 用例回归、一键复现、仓位理由阐明均已落地。对照两个外部输入，诊断出以下瓶颈：

| # | 瓶颈 | 现状 | 外部对照 |
|---|---|---|---|
| B1 | **新闻噪声未过滤** | NewsCollector 采集后直接做情绪统计（如"板块景气度=18 条新闻情绪分 60"），命中关键词 ≠ 语义相关 | 得物实测召回噪声 60%（5 条仅 2 条相关），结论："不只靠搜，更要靠筛" |
| B2 | **检索查询单一** | 新闻采集用板块名/周期关键词单变体查询，搜狗 403 时无备选表述 | 得物：自然语言多变体并行检索，互补覆盖意图各侧面 |
| B3 | **材料不足无补救** | researcher 固定采集-分析一遍过，材料缺失只能降级模板段 | 得物：自主评估（相关性/完整性/时效性）→ 不足则精读/补搜/交叉验证 |
| B4 | **来源权威无分级** | CitationChecker 白名单是二元判定（权威/非权威） | 得物：来源权威性排序 + 冲突时明确标注采信来源 |
| B5 | **评级词汇分散** | 公司研报"买入/增持"、行业研报"超配/标配"、investor 隐式评级，多文件各写各的 | TradingAgents：`rating.py` 统一五档词表 + 确定性解析器，防多调用点漂移 |
| B6 | **决策缺对抗视角** | 打分 → 排序 → 配仓单链路，入选/剔除理由无反方质询 | TradingAgents：Bull/Bear + 三方风控双层辩论 |
| B7 | **无历史复盘** | 混合记忆只有方法论 RAG，决策结果无闭环 | TradingAgents：pending → 实际收益 → 反思 → 注入下次运行 |
| B8 | **LLM 单点无容灾** | MiniMax 单模型，限流/超时即降级模板 | 得物：5xx/限流/超时自动切备用模型，4xx 不切换 |
| B9 | **配置静默兜底** | 多处 `config.get(key, default)`，错配不暴露 | TradingAgents：fail-loud，非法值启动即抛错 |
| B10 | **量化评测缺失** | 只有单元/集成测试，无"报告质量/决策准确率"评测集 | 得物：三组评测集全量人工标注（77.8%/93.8%/95.2%） |

---

## 二、优化方案（按来源标注，落地到具体模块）

### 方案 1：新闻三阶段质量过滤 Pipeline ⭐（对应 B1，来源：得物）

**问题**：情绪统计直接把全部召回新闻计入，噪声新闻（关键词命中但主题无关，如"消费板块"搜到"消费者权益投诉"）稀释/扭曲景气度判断——这与研报"逻辑严谨性"评审维度直接冲突。

**设计**（在 `news_collector.py` 采集后、情绪统计前插入）：

```
Stage 0 FastPass：结果 ≤ 2 条且来源全在 RELIABLE_SOURCES → 直接通过（零开销）
Stage 1 规则粗筛：标题/摘要须同时含标的（公司名/板块核心词）与主题词
                  （财报/业绩/行业类），过滤纯词面命中
Stage 2 LLM 精评（可开关）：逐条打分（相关 0.8 / 部分相关 0.5 / 无关 0.1），
                  阈值 0.4，仅在新闻数 > 5 时触发，控制 Token 成本
```

- 被过滤新闻数写入 `run_stats.json`（如 `news_filtered: 7/18`），审查与答辩可解释"为何用 11 条而非 18 条"
- 每阶段独立可开关（config: `news_filter.rerank_enabled/llm_grade_enabled`），复现场景可全关保持确定性

### 方案 2：查询多变体扩展 ⭐（对应 B2，来源：得物）

**问题**：单关键词查询召回窄且易被反爬单点卡死（已发生过搜狗 403）。

**设计**：`news_collector.py` 查询构造从"1 个关键词"改为"3 个自然语言变体"并行采集后去重合并：

| 变体类型 | 示例（消费板块） |
|---|---|
| 实体型 | "消费板块 上市公司 业绩" |
| 事件型 | "消费品行业 最新动态 政策" |
| 完整句型 | "2026 年消费行业上市公司的经营情况如何" |

得物经验：embedding 模型对完整句子的语义编码优于关键词串；变体互补覆盖（流程/政策/入口各侧面）。去重沿用"按 sourceId/URL 跨变体合并"（对应得物 `SourceCollector` 思路）。

### 方案 3：材料三维评估与补救循环 ⭐（对应 B3，来源：得物）

**问题**：材料缺失时当前只能写降级模板段（已有 SECTION_SOURCE 标注，但内容空洞），评审会看到"数据不足"的痕迹。

**设计**：`report_writer` 撰写前对每章材料做轻量三维检查（规则判定，不走 LLM）：

- **相关性**：材料 JSON 中目标标的/板块名出现次数
- **完整性**：该章必需字段是否非空（如"景气度分析"章须有景气结论+新闻数）
- **时效性**：财报期/新闻日期是否超过阈值（如 90 天）

不足时按"指南针而非地图"触发一次补救：①RAG 精读（拉方法论全文而非片段）；②换源补采（缓存内其他 collector）；仍不足才降级模板段，并在该段标注"数据缺失原因"——把"降级"变成"可解释的决策"。

### 方案 4：来源权威分级与冲突标注（对应 B4，来源：得物）

将 `citation_checker.py` 的白名单二元判定升级为有序权威表：

```
国家统计局/交易所公告 > akshare 财报数据（组委会公司池）> 财联社等权威财经媒体 > 一般来源
```

- claim 命中高权威来源时审查加分权重更高
- 同一事实多来源数值冲突时（如两媒体报道的营收不一致），研报中标注"数据存在差异，采信 akshare 财报口径"——直接增强"逻辑严谨性"得分点

### 方案 5：统一评级词汇表模块（对应 B5，来源：TradingAgents）

新建 `common/rating.py`：

- 公司评级：买入/增持/持有/减持/卖出（五档）
- 板块配置：超配/标配/低配（三档）
- 仓位决策：full/partial/empty（与 decision.json 对齐）
- 提供 `parse_rating(text, default)` 确定性解析器

writer/reviewer/investor 三处改为 import 同一模块，Reviewer 结构校验不再各文件硬编码词汇——杜绝此前按类型区分校验时的口径漂移类返工。

### 方案 6：边界标的 Bull/Bear 论证（对应 B6，来源：TradingAgents，决赛项）

investor 阶段对**阈值 ±10 分**的边界标的（入选线附近）各生成一段多/空论证（单次 LLM 调用），论证要点写入 `decision.json` notes。成本：全池最多 5-8 个边界标的 ×1 次调用；收益：赛题"阐明决策逻辑"从"规则陈述"升级为"对抗权衡后的结论"，答辩叙事显著增强。**初赛不动主干，决赛落地。**

### 方案 7：反思记忆闭环（对应 B7，来源：TradingAgents，决赛项）

`decision_log/` 增加 reflection 层：历次 Portfolio 快照 + 标的后续区间表现对比，LLM 生成 2-4 句教训摘要（仿 TradingAgents Reflector 提示词结构：方向对错→论点成败→一条具体教训），回注 researcher 材料。竞赛为一次性场景，价值在**架构完整性展示与答辩素材**。

### 方案 8：LLM 主备容灾 ⭐（对应 B8，来源：得物）

**问题**：撰写链路依赖 MiniMax 单模型，限流/超时时整段降级模板，质量不可控。

**设计**：`common/` LLM 调用层增加容灾策略（环境已有智谱 Key，现仅用于 embedding）：

- 可重试错误（5xx/限流/超时/网络异常）→ 自动切智谱 GLM 备用模型重试一次
- 4xx（参数/内容错误）→ 不切换，直接降级并留痕（避免掩盖真实错误）
- 切换事件写入 `run_stats.json`（`llm_failover: 2`），资源消耗数据保持完整

### 方案 9：fail-loud 配置校验（对应 B9，来源：TradingAgents）

技能入口（`run_report.py` 初始化）增加启动校验：`score_threshold`（0-100 数值）、`max_weight`（0-1）、`pool_file`（存在性）等关键配置类型/值域非法时**立即报错退出**，替代静默 `.get()` 兜底——无人值守复现时错配第一时间暴露，而不是产出一份错口径的 Portfolio。

### 方案 10：量化评测集（对应 B10，来源：得物，决赛项）

构建小型人工标注评测集（10-15 例）：给定标的数据快照 → 期望评级区间/关键结论要点，量化两个指标：

- 打分准确率（规则分与人工评级的一致率）
- 研报要点覆盖率（审查 100 分之外的事实正确性）

初赛时间不足，列为决赛答辩的"质量证据"——得物 77.8%/93.8%/95.2% 的人工标注数字正是其可信度来源。

### 方案 11：低成本工程细节（来源：TradingAgents + 得物）

- **原子写**：decision.json/scores_cache/run_stats 写入改 `tmp + os.replace()`，防中断损坏
- **数据截至日期强制标注**：研报每章材料统一注入"数据截至 YYYY-MM-DD"（防幻觉锚定，TradingAgents #814 思路）
- **中间产物落盘**：writer 各章草稿与 reviewer 各轮意见落盘 `decision_log/drafts/`，自检反馈循环证据链完整化

---

## 三、优先级与路线图

### P0：初赛窗口内（8/20–8/23，低风险、当天可验证）

| 项 | 预估工作量 | 验证方式 |
|---|---|---|
| 方案 9 fail-loud 校验 | 0.5 天 | 单测 + 复现脚本 |
| 方案 11 原子写 + 数据截至日期 | 0.5 天 | 回归 |
| 方案 5 统一评级模块 | 0.5 天 | 现有 241+ 用例回归 |
| 方案 1 新闻过滤（仅 Stage 0+1 规则层，LLM 层默认关） | 1 天 | 重跑行业研报对比情绪分变化，审查须仍 100 分 |
| 方案 8 LLM 主备（若智谱 chat Key 可用） | 1 天 | 故障注入测试 |

> P0 原则：**不改决策主干与已达标产物口径**；方案 1 若影响已交付研报数据，保留开关默认关闭，仅随代码提交能力。

### P1：决赛阶段

- 方案 6 边界标的 Bull/Bear 论证（决策叙事核心增强）
- 方案 7 反思记忆闭环（架构完整性展示）
- 方案 10 量化评测集（质量证据）
- 方案 3 完整补救循环（含 LLM 精读）
- 方案 2 查询多变体（需重新采集验证缓存一致性）

### P2：不采纳项及理由

| 外部能力 | 不采纳原因 |
|---|---|
| SSE 多实例断点续传（得物） | 本项目为 CLI 批处理，无长连接交互场景；已有采集级断点续采 |
| 多模态截图提问（得物） | 赛题交付为文本研报，无图片输入需求 |
| 全面 LLM 结构化输出 schema（TradingAgents） | 交付物以 markdown 为主，现有正则解析已稳定，重构收益低于风险 |
| 快慢双 LLM 全链路分级 | MiniMax 单一接入下无轻量型号可选；待决赛接入多供应商后再评估 |

---

## 四、风险与回归保障

1. **口径漂移风险**：方案 1/2 改变新闻输入 → 情绪分/景气度可能变化 → 已交付 8 份研报与 Portfolio 的生成口径须保持一致。**对策**：新能力默认开关关闭 + 复现脚本锁定旧口径参数，验证通过后再评估是否刷新交付产物。
2. **引用率闸门联动**：新闻过滤后 claim 数量变化，须重跑 CitationChecker 验证 ≥90% 仍满足。
3. **确定性承诺**：所有新增 LLM 环节（方案 1 Stage 2、方案 6、方案 7）必须可关闭，关闭后全链路与当前确定性口径逐字节一致——这是一键复现脚本的信任基础。
4. **验证流程**：每个方案独立 commit + finance 全量回归（当前基线 243 passed）+ 校验器 PASS + 复现脚本端到端抽查。

---

## 五、结论

两个外部输入指向同一主线：**答案/决策质量 = 召回全 + 筛选准 + 整合有判断力 + 过程可解释**（得物），以及 **对抗与反思让决策叙事严谨**（TradingAgents）。

本项目已具备"规则骨架 + 质量闸门 + 可复现"的底盘，缺的正是**信息筛选层（B1/B2/B3/B4）与决策对抗层（B6/B7）**。初赛 3 天窗口聚焦 P0 五项低风险加固；决赛阶段以 Bull/Bear 论证 + 反思记忆 + 评测集构成"决策严谨性"的完整证据链——这与赛题"阐明决策逻辑"和决赛"逻辑严谨性、答辩"的评审导向完全对齐。
