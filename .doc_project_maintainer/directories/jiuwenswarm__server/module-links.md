# Module Links

- `agentserver-runtime`: primary owner.
- `gateway-and-channels`: interacts through WebSocket wire protocol and server push.
- `agent-harness`: runtime execution behind `AgentManager`.
