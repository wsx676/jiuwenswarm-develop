# JiuwenSwarm 金融分析 Agent 项目评审报告

> 评审时间：2026-08-19 ｜ 评审基准：commit `18ea327`（Day 5 三任务交付）
> 评审对象：`jiuwenswarm/resources/agent/workspace/skills/finance-report/`（比赛交付技能）+ `tests/unit_tests/finance/` + `reports/finance-report/`（运行产物）
> 评审方法：全量精读 Day 5 新增/修改代码，抽查 Day 1-4 存量模块（collectors/analyzers/generators/common），核验运行产物闭环一致性与测试基线

---

## 一、总体结论

**综合评级：A-（88/100）**

项目已具备完整的端到端交付能力：公司池批量采集 → 确定性因子评分 → 风控约束仓位配置 → 入选标的研报生成，全链路产物（Portfolio.json、8 份个股研报、决策日志、遥测记录）齐备且相互一致。Day 5 三项任务（Swarmflow 工作流、遥测与可复现性、全池批量运行）完成质量高，测试基线 237 passed，SwarmFlow 校验器 0 error。

主要短板集中在：**存量 CLI 参数组合的边界一致性**（评分缓存与板块过滤的交互、自定义池与 Planner 口径脱节）、**行业/宏观研报仍为占位实现**、以及若干低风险健壮性细节。无高危（H）问题。

| 维度 | 得分 | 说明 |
| --- | --- | --- |
| 架构设计 | 92 | 五阶段状态传递清晰，采集/分析解耦可复用，批量容错设计合理 |
| 功能完整度 | 85 | 公司研报+投资决策闭环完整；行业/宏观研报为占位 |
| 代码质量 | 88 | 降级路径完备、注释留痕规范；少量副作用式写参和冗余判定 |
| 测试质量 | 90 | 18 文件 237 用例全过，Day 5 新增 25 用例覆盖三任务验收点 |
| 可复现性 | 93 | 固定种子+确定性规则+缓存复放+环境说明，实测两次分析评分完全一致 |
| 交付物合规 | 90 | 白名单/权重约束/引用闸门全部通过；决策日志缺时间戳 |

---

## 二、交付物核验（实测）

### 2.1 公司池与数据资产

- 公司池：6 板块 / **49 个标的**（金融 8、消费 9、新能源/电力 8、科技/AI/半导体 12、周期/资源 8、高端制造/基建 4）
- 采集缓存 `data/`：**49/49 标的全覆盖**（quote/filing/news 三类），采集阶段 `failed: {}` 零失败
- 评分缓存 `scores_cache.json`：与公司池白名单**完全一致**（无多出、无缺失），Top 评分：603986（100）、601168（94）、601899（91）、300750（88）、600426（87）

### 2.2 投资组合与决策日志

- `Portfolio.json`：8 只持仓（603986/601168/601899/300750/600426/300308/600309/601600），总权重 0.99 ≤ 1.0，单标的最大 0.14 ≤ 0.4，**全部在公司池白名单内**
- `Portfolio.json` 与 `decision_log/decision.json` 的 portfolio 字段**逐项一致**
- 决策日志含「持仓集中度：达标标的 32 只，按评分取前 8 只配置」调整依据留痕（Day 5「任何调整须回写决策日志」要求达标）

### 2.3 研报产物

- 8 份入选标的研报全部生成，**审查得分均 100 分、0 问题**（自研 Reviewer 四类校验：溯源/图文一致/结构/合规）
- 抽查 603986.md：八章节结构完整（核心观点→数据来源），图片引用 `charts/price_603986.png`、`charts/margin_603986.png` **均真实存在**（无失效引用）

### 2.4 遥测与可复现性

- `run_stats.json`：10 次运行滚动记录（上限 10），含各阶段耗时/LLM token/失败留痕/种子
- LLM 资源消耗合计：81 次调用 / 55,811 input tokens / 48,666 output tokens（单标的研报全流程约 9 次调用）
- 确定性验证：两次独立运行分析阶段评分**完全一致**（缓存复放）；全流程打分为确定性规则，无随机源（SEED=20260819 已固定并落盘）
- `reports/finance-report/README.md` 含依赖清单、.env 配置表、五步复现命令，第三方可重放

### 2.5 质量门禁

- 回归测试：`tests/unit_tests/finance/` **237 passed**（18 个测试文件）
- SwarmFlow 校验器（swarmskill-creator）：**PASS，0 error / 2 warning**（warning 为扇出上限声明的提示性检查，已按规范声明 MAX_FANOUT/MAX_REPORTS 并 log 跳过项）

---

## 三、发现的问题

### M 级（建议修复，影响正确性/一致性）

**M1. `invest --use-cached-scores` 与 `--sector` 组合时板块过滤失效**
- 位置：`agents/investor.py` `run_portfolio()` L179-205
- 现象：`scores` 参数非空时直接复用传入的全量评分，`sector` 板块过滤只在实时评分分支（`scores is None`）内生效。CLI 同时传 `--use-cached-scores --sector 消费板块` 时，会以全池评分参与配置，产出含非目标板块标的的组合。
- 影响：单板块批量决策场景的组合越界（白名单校验仍通过，因为是全池白名单，属于语义违背而非提交违规）。
- 建议：`scores` 非空时按板块标的集过滤后再进 `_allocate`；同时 `--use-cached-scores` 与 `--sector` 组合时在 CLI 层提示缓存口径（全池/单板块）。

**M2. `--pool-file` 自定义池与 Planner/行业分析口径脱节**
- 位置：`agents/planner.py` L69（`pool_file = self.config.get("pool_file", DEFAULT_POOL_FILE)`）+ `run_report.py` invest 分支（config 未注入 `pool_file`）
- 现象：Investor 的白名单与板块过滤使用 CLI 传入的 `--pool-file`，但 Planner（行业分析竞对提取、板块归属）始终加载默认池。自定义池时两套口径不一致。
- 影响：赛题场景池固定为 `example/上市公司列表.xlsx`，实际触发概率低；但作为可复用技能存在逻辑瑕疵。
- 建议：`run_report.py` 构造 config 时透传 `pool_file`；或 Planner 支持 `plan()` 时由调用方注入池对象（`collect_pool`/`score_pool` 已持有过滤后的池，可随 `ReportRequest` 下传，还能顺带消除每标的重复解析 xlsx 的开销）。

**M3. `industry` / `macro` 子命令为占位实现**
- 位置：`generators/report_writer.py` L91-94（非 company 返回「（待生成）」骨架）；`SKILL.md` 任务表仍宣传三种研报类型
- 现象：CLI 与 SKILL.md 均提供行业/宏观研报入口，实际产出占位文档（含标题 + 待生成字样）。
- 影响：若评委或用户按文档调用会得到空壳产物，观感减分。
- 建议（二选一）：① SKILL.md/CLI help 标注「当前版本聚焦公司研报，行业/宏观为规划中」；② 复用现有 IndustryAnalyzer/MacroAnalyzer 数据链补最小可用实现（八段模板降级路径已具备，增量成本可控）。

**M4. 分析阶段批量评分时无条件渲染图表**
- 位置：`agents/researcher.py` `_analyze()` L178-191；调用链 `score_pool → analyze_cached → _analyze`
- 现象：批量分析 49 标的时每个标的渲染 price/margin 两张 PNG（matplotlib），而决策只消费 `finance_analysis`/`quote_data`。全池分析产生 ~100 张图表，成为该阶段主要耗时（且在研报阶段会再次渲染）。
- 影响：批量分析阶段性能浪费（分钟级）；charts/ 目录混入与研报无关的中间产物。
- 建议：`_analyze` 增加 `with_charts` 开关，`analyze_cached`（评分路径）跳过图表渲染；或延迟到报告阶段独占。

### L 级（可选修复，健壮性/规范细节）

**L1. `call_with_retry` 未捕获 agent 异常**
- 位置：`scripts/workflow.py` L212-227
- 说明：重试只覆盖「返回值 verdict≠ok」路径；若 `agent()` 直接抛异常（超时等），异常将穿透重试封装。SwarmFlow 文档对 `parallel` 明示「失败项为 None，调用不抛错」，`pmap` 未明示同等语义。建议 for 循环体内包 `try/except`，把异常也纳入「重试一次→降级」的统一路径。

**L2. Reviewer 合规检查与结构检查重复计分**
- 位置：`agents/reviewer.py` L129-148
- 说明：「风险提示」缺失会同时被 `_check_structure` 和 `_check_compliance` 各计 1 个 issue（-20 分）；且 `passed = len(issues) == 0 and score >= 70.0` 中后者在前者成立时恒真（冗余）。建议去重计分、简化判定。

**L3. `resolve_pool_file` 静默回退默认池**
- 位置：`run_report.py` L51-59
- 说明：显式传入不存在的路径只打印警告即回退默认池跑全池（Day 5 实测中技能根 cwd 相对路径即触发）。工作流场景该回退是刚需，但 CLI 直跑场景建议对「用户显式传错路径」提供 fail-fast 选项（如 `--strict-pool`）。

**L4. 决策日志缺时间戳**
- 位置：`agents/investor.py` `_save()` L292-315
- 说明：`decision.json` 无 `generated_at` 字段，多次覆盖后仅能靠文件 mtime 或 `run_stats.json`（追加式）对齐运行批次。建议写入时间戳（与 `run_stats` 同源），提升第三方复现比对的可追溯性。

**L5. `run_investment` 报告阶段无扇出上限**
- 位置：`orchestrator.py` L237-253
- 说明：`invest --save`（不带 `--skip-reports`）时为全部入选标的串行生成研报，无 `MAX_REPORTS` 类上限（workflow.py 已有 MAX_REPORTS=16，CLI 路径没有）。不设 `--max-positions` 且达标标的多时（如 32 只）将串行生成 32 份。建议与工作流口径对齐或文档提示配合 `--max-positions` 使用。

**L6. workflow 报告阶段命令不带 `--name`**
- 位置：`scripts/workflow.py` `REPORT_PROMPT` L130 vs 实测批量命令（带 `--name`）
- 说明：公司名依赖 `_write_company` 的 `quote.get("name")` 缓存回填（`report_writer.py` L108，实测有效）与 news 采集的 `name or symbol` 降级。在「先采集后报告」的工作流顺序下无实际问题；直接对无缓存标的跑 company 时新闻检索质量降级。建议 REPORT_PROMPT 补 `--name`（决策阶段 portfolio 上下文中可得）。

**L7. 其余代码细节**
- `report_writer._assemble` 通过 `data["image_issues"] = issues` 副作用式写参（`report_writer.py` L466-467）
- `researcher.research()` 中 citations 先被 `_analyze` 结果覆盖再重建（L76-80），路径绕但行为正确
- `RunStats` 非线程安全（当前全流程单线程串行，无碍）；`fix_random_seed` 未覆盖 numpy（当前无 numpy 随机源，注释已声明边界）
- `orchestrator.collect_pool` 中 `planner.plan()` 在重试 try 块之外（planner 为纯规则，风险极低）
- `data/` 缓存无过期机制（`collected_at` 已留痕，README 已声明按需删除重采，可接受）

---

## 四、分模块评审

### 4.1 Swarmflow 工作流（`scripts/workflow.py`，Day 5 新增）★ 92

- **结构合规**：META 纯字面量五阶段、`run()` 以 `parse_args` 开头、顶层 `phase()` 顺序覆盖、无非确定性导入/print/文件 IO——与校验器规则完全对齐，实测 PASS。
- **设计亮点**：五个 `retry_*` 内嵌函数把重试封装收在 `run()` 内部，既满足校验器的顶层扫描约束，又保留 agent 调用的字面量 label/phase；`call_with_retry` 用 for-range 实现有限重试（MAX_ATTEMPTS=2），杜绝无限循环。
- **状态传递**：提示词明确指示各阶段 CLI 命令与产物位置（data/ → scores_cache.json → --use-cached-scores → 入选标的研报），阶段失败有明确降级路径（分析失败→决策回退实时采集；决策失败→终止留痕；空仓→跳过报告）。
- **不足**：见 L1（异常路径）、L6（--name）。

### 4.2 遥测与可复现（`common/telemetry.py` + 接入点，Day 5 新增）★ 90

- `_PhaseTimer` 上下文管理器不吞异常且失败同样留痕（error 标志 + failures 记录），设计干净。
- `run_stats.json` 追加式保留最近 10 次，实测滚动正确；LLM token 累计经 `LLMClient.chat` 的 usage 提取自动完成（try/except 隔离，遥测失败不影响主流程）。
- 可复现性论证充分：确定性规则 + 固定种子 + 缓存复放 + 环境说明，两次独立运行评分完全一致已实测验证。
- 不足：无时间戳贯穿（L4）、单线程假设未显式声明。

### 4.3 批量运行与容错（`orchestrator.py` / `investor.py` / `researcher.py`，Day 5 修改）★ 89

- **重试留痕三通道**：决策日志 notes + `RUN_STATS.failures` + logger warning，测试覆盖「恰好重试一次」「瞬时失败恢复」「跳过留痕」三种情形。
- **评分缓存状态传递**：`scores` 参数跳过 research_fn 的路径有测试守护（`_must_not_be_called` 断言），缺失自动回退实时采集。
- **持仓集中度规则**：`max_positions` 截断 + 决策日志依据留痕，解决 32 只微权重过度分散问题，实测 8 只组合权重分布合理（0.11-0.14）。
- **两阶段拆分**：`collect_only`/`analyze_cached` 缓存优先语义清晰，`_load_cache` 缺失返回空不重采（由采集阶段兜底），`_collect` 的 H2 回归（缺市值重采升级）保留完好。
- 不足：见 M1（sector×scores 交互）、M4（图表渲染）、M2（planner 池口径）。

### 4.4 CLI 入口（`run_report.py`）★ 88

- 六子命令结构清晰；`--output-dir` SUPPRESS 双层挂载解决了历史参数覆盖陷阱；缺省输出目录锚定项目根消除了技能根 cwd 误落产物问题（Day 5 实测踩坑后修复）。
- `invest` 分支的评分缓存读取有损坏回退与缺失回退双兜底。
- 不足：见 M2、L3、L5；`research --stage collect` 的 `--save` 仅保存 run_stats（行为如文档所述，无歧义）。

### 4.5 存量模块抽查（Day 1-4 产物）★ 87

- **collectors**：news_collector 迭代式 Deep Research 双保险（max_depth + max_items + 信息饱和判断）与三源降级（搜狗→新浪滚动→Bing）健壮；LLM 精炼失败降级规则法路径完整（temperature 透传已在 Day 5 修复）。pool_loader 白名单/板块/竞对工具函数职责单一。
- **analyzers**：finance_analyzer 披露口径取数（不自行计算）、同比取上年同期、缺数据 None 语义贯穿（不写 0.0 哨兵）、报告期升序排序——前次评审问题（时间排序、值域）修复保持完好。
- **generators**：report_writer 分治式生成（大纲→逐段带前文摘要）+ LLM 降级模板段（SECTION_SOURCE 汇总标注）+ 图片本地化校验；citation_checker 段落级引用闸门与权威白名单口径与 Reviewer 统一。
- **图表**：图文同源（图表 data 与正文材料同一 dict）、中文字体配置、缺失值 NaN 不绘制。
- 不足：report_writer 占位实现（M3）、`_rating` 简版评级与 Investor 正式决策双口径并存（研报内评级与 Portfolio 权重可能表述不一致，已有注释说明分工，可接受）。

### 4.6 测试与文档 ★ 90

- 18 个测试文件 237 用例，覆盖 agents 接线、五类 collector、四类 analyzer、三个 generator、telemetry、批量容错、CLI argparse、workflow 结构合规（AST 级自检与校验器对齐）。测试不触网（缓存预置/monkeypatch/mock session）。
- SKILL.md v2.1.0 结构完整（frontmatter kind、目录树、使用方式、Workflow、Files 表）；README 环境说明与复现步骤可直接照跑。
- 不足：测试对 M1（sector×scores）场景未覆盖——若补充该用例可在回归中拦截。

---

## 五、亮点摘录

1. **确定性优先的架构取向**：评分与仓位分配是纯规则（无 LLM、无随机），LLM 只用于研报撰写增强——把「可复现」做在了机制上而非文档承诺上。
2. **阶段间状态传递的工程化**：data/ 缓存（断点续采）→ scores_cache.json（评分复用）→ 决策 → 仅为入选标的生成报告，批量运行的总成本随阶段推进收敛。
3. **失败留痕的完整性**：单标的失败三通道留痕（决策日志/遥测/日志），批量失败不阻断且有测试守护「恰好重试一次」。
4. **质量内环**：CitationChecker 引用闸门（≥90%）+ 权威来源白名单 + 图文同源校验 + 审查回流 ≤2 轮，8/8 研报审查 100 分是机制的自然结果。
5. **前次评审修复的持续性**：H1-H3/M1-M5/L1-L4 修复（如时间排序、值域校验、None 语义、引用率口径统一）在本次抽查中全部保持完好，无回归。

---

## 六、结论与建议

项目达到「可提交」状态：交付物完整一致、质量门禁全绿、可复现性有实测背书。当前唯一阻塞项是 **commit `18ea327` 尚未推送远程**（本机代理 127.0.0.1:7993 未运行，直连 GitHub 被 reset；代理恢复后推送即可）。

修复优先级建议：

| 优先级 | 项 | 预估工作量 |
| --- | --- | --- |
| 推送前顺手修 | M1（sector×评分缓存过滤）+ 补 1 个回归用例 | 0.5h |
| 推送前顺手修 | L4（决策日志时间戳） | 0.2h |
| 建议跟进 | M3（行业/宏观占位标注或最小实现） | 0.5h（标注）/ 1d（实现） |
| 建议跟进 | M4（分析阶段跳过图表渲染） | 0.5h |
| 低优 | M2（planner 池口径透传） | 0.5h |
| 低优 | L1/L2/L3/L5/L6/L7 | 各 ≤0.3h |

---

## 七、修复记录（2026-08-19）

评审问题已全部处置完毕，修复明细如下：

| 问题 | 处置方式 | 涉及文件 |
| --- | --- | --- |
| M1 | ✅ 已修复：sector 过滤后同步收窄白名单 + 缓存评分路径显式过滤，补回归用例 `test_cached_scores_respect_sector` | `agents/investor.py`、`tests/.../test_investor_batch.py` |
| M2 | ✅ 已修复：`invest`/`research` 分支将 `pool_file` 注入 config（orchestrator/planner 共享 dict 引用，运行时生效） | `run_report.py` |
| M3 | ✅ 已处置（标注方案）：SKILL.md 任务表与使用方式标注「规则模板版」并说明当前聚焦公司研报闭环 | `SKILL.md` |
| M4 | ✅ 已修复：`_analyze` 增加 `with_charts` 开关，`analyze_cached`（评分路径）传 False 跳过图表渲染 | `agents/researcher.py` |
| L1 | ✅ 已修复：`call_with_retry` 捕获 agent 异常纳入重试路径，不再穿透中断 pmap 扇出 | `scripts/workflow.py` |
| L2 | ✅ 已修复：`_check_compliance` 移除与 `_check_structure` 重复的「风险提示」检查；`passed` 冗余阈值条件简化 | `agents/reviewer.py` |
| L3 | ⏭️ 保留现状：工作流场景回退为刚需，维持现有警告 + 回退行为 | `run_report.py` |
| L4 | ✅ 已修复：`decision.json` 写入 `generated_at` 时间戳（与 run_stats 同口径），补测试断言 | `agents/investor.py` |
| L5 | ⏭️ 保留现状：CLI 直跑由用户通过 `--max-positions` 控制，工作流路径已有 MAX_REPORTS 上限 | `orchestrator.py` |
| L6 | ✅ 已缓解：REPORT_PROMPT 补充名称回填机制说明（公司名由行情缓存/公司池自动回填，「先采集后报告」顺序下无实际问题） | `scripts/workflow.py` |
| L7 | ⏭️ 保留现状：均为不影响正确性的代码风格细节（副作用写参/非线程安全等），留作后续重构 | — |

**修复后质量门禁**：
- 回归测试：**238 passed**（原 237 + M1 新增用例 1）
- SwarmFlow 校验器：**PASS，0 error / 2 warning**（与修复前基线一致，warning 为既有扇出上限提示性检查）

---

*附：评审证据快照——237 tests passed；校验器 [PASS] 0 error；data 49/49 覆盖；scores_cache 与池零差异；Portfolio.json ≡ decision.json（总权重 0.99）；8/8 研报审查 100 分、图片引用全部有效；run_stats.json 10 次滚动记录（LLM 合计 81 calls / 104k tokens）。*
